{
  "name": "html-site",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p ./public && cp -rf ./view/*.html ./public/",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.51.0"
  }
}
